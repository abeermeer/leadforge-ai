name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    env:
      DEBUG: "true"
      SECRET_KEY: ci-secret-key
      FERNET_KEY: NnqnTIDsmStD-diXZmHUQAVF3SUa90nm5zOLQC79xyA=
      DATABASE_URL: sqlite:///./ci.db
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Migrate
        run: alembic upgrade head
      - name: Tests (with coverage gate)
        # Threshold sits just under the current app-only figure so the gate
        # catches a real regression without failing on noise. Ratchet it up as
        # coverage grows; never lower it to make a red build pass.
        run: pytest -q --cov --cov-report=term-missing --cov-fail-under=55
      - name: Dependency audit (non-blocking)
        run: pip install pip-audit && pip-audit || true

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci
      - run: npm run build
      - name: npm audit (non-blocking)
        run: npm audit --production || true
